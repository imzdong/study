plugins {
	id "com.vanniktech.maven.publish" version "0.19.0" apply false
	//false，表示当前项目不需要应用该插件，只是为了让其他项目可以应用该插件
}

allprojects {
	plugins.withId("com.vanniktech.maven.publish") {
		mavenPublish {
			sonatypeHost = "S01"
		}
	}
}